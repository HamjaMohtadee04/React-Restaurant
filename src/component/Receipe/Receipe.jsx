import PropTypes from 'prop-types';
const Receipe = ({receipe}) => {
    console.log(receipe)
    return (
        <div>
            
        </div>
    );
};

Receipe.propTypes={
    receipe: PropTypes.object
}
export default Receipe;